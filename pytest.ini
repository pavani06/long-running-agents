[pytest]
addopts = --import-mode=importlib
markers =
    validator_integration: exercises scripts/validate-obsidian.ts for real (needs Node + npm install); run by the Check Obsidian Conventions workflow
